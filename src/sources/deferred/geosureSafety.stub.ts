// GeoSure requires a commercial/partner API key — no self-serve tier exists.
// Deferred for MVP; static_safety_notes.json (see numboCrime.stub.ts) covers
// the same "neighborhood safety" need at zero cost until this is justified.
export {};